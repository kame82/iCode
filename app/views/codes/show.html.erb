<%= render 'devise/shared/error_messages', resource: @code %>
<div class="container mx-auto px-5" data-controller="read-only-editor">
  <%= render partial: 'codes/showForm', locals: { model: @code } %>
  <%= render partial: 'codes/editorComponent' %>
</div>

<%= render'codes/scriptForIframe' %>
<% assign_meta_tags title: "#{@code.title}",
                    image: @code.image_url
                    %>
